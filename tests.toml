test_format = 1.0

[default]

    # ------------------------------------------------------------------------
    # Lingua 90 — package_check test suite
    # ------------------------------------------------------------------------

    exclude = ["install_private"]   # the app has its own profile system

    [default.test_upgrade_from]
        # Add previous published versions here once you have releases, e.g.
        # [default.test_upgrade_from.0.1.0~ynh1]
        # name = "Upgrade from 0.1.0"
